# PR Benchmark Suite

This directory holds the small, stable benchmark scenarios intended for pull
request regression detection.

The PR suite should remain low-noise, deterministic, and fast enough to run as
part of normal CI while still exercising the real engine and plugin path end to
end.
